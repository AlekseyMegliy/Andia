
name: Build and deploy


on:
  push:
    branches: [ main ]
 

# выполняемые действия
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    # запускаются по шагам
    # name - название шага
    # в github actions будет выводиться название шага, и процессы
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2.3.1
        with:
          persist-credentials: false

      - name: Install 🔧
        run: npm install

      - name: Build 👷
        run: npm run build

      - name: Deploy 🚀
#         run: npm run deploy
        uses: AlekseyMegliy/github-pages-deploy-action@3.7.1
        with:
     
          GITHUB_TOKEN: ${{ secrets.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 }}
          BRANCH: gh-pages
          FOLDER: public
          CLEAN: true
